const assert = require("assert");

const { generateTicket, generateOutput, generateLine } = require("./tickets");

describe("generate ticket", function() {
  it("should return a new ticket with 2 lines", function() {
    // assert.generateTicket(2).shouldReturnAnObject
  });
});
